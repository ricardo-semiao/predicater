% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{attributes-dimensions}
\alias{attributes-dimensions}
\alias{n_dims}
\alias{n_dimnames}
\alias{has_dimnames}
\alias{has_rownames}
\alias{has_dim}
\title{Attributes - Dimensions existence and sizes}
\usage{
n_dims(x, empty = TRUE, how = "dim")

n_dimnames(x, invalid = TRUE, how = "dimnames")

has_dimnames(x, n = NULL, how = "dimnames")

has_rownames(x, how = "dimnames")

has_dim(x, n = NULL, how = "dim")
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{empty}{[\code{TRUE} | \code{FALSE}] Whether to count empty dimensions and
dimension names.}

\item{how}{[\code{character(1)}]
How to extract the attribute:
\itemize{
\item For dimensions: \code{"dim"} for \code{\link[=dim]{dim()}} and \code{"attr"} for \code{attr(x, "dim")}.
\item For dimension names: \code{"dimnames"} for \code{\link[=dimnames]{dimnames()}} and \code{"attr"} for
\code{attr(x, "dimnames")}.
\item For rownames: \code{"rownames"} for \code{\link[=rownames]{rownames()}}, \code{"dimnames"} for the first
element of \code{\link[=dimnames]{dimnames()}}, and \code{"attr"} for \code{attr(x, "row.names")}.
}}

\item{invalid}{[\code{TRUE} | \code{FALSE}] Whether to count dimensions with NA,
empty, or duplicate names.}

\item{n}{[\code{integer(1)}] Number of dimensions to check for. Set to \code{NULL} to
not test.}
}
\value{
\itemize{
\item [integer(1)] For \code{n_dims()} and \code{n_dimnames()}.
\item [\code{TRUE} | \code{FALSE}] For \code{has_dim()}, \code{has_dimnames()}, and
\code{has_rownames()}: the sacalar result of the test.
}
}
\description{
Functions for the presence and number of dimensions and dimensions names of
an object.
\itemize{
\item \code{n_dims()} and \code{n_dimnames()} return the number of dimensions and dimension
names, respectively, controlling for empty dimensions and invalid names.
\item \code{has_dim()}, \code{has_dimnames()}, and \code{has_rownames()} return whether the
object has dimensions, dimension names, and rownames, respectively.
}
}
\examples{
x <- structure(
  1:6, dim = c(2, 3, 1),
  dimnames = list(c("a", "a"), c("x", "y", "z"), NULL)
)

# Counting dimensions and dimension names:
n_dims(x) #> 3
n_dims(x, empty = FALSE) #> 2

n_dimnames(x) #> 2
n_dimnames(x, invalid = FALSE) #> 1 (rownames have duplicates)

# Testing for dimensions and dimension names:
has_dimnames(x) #> TRUE
has_rownames(mtcars) #> TRUE
has_dim(mtcars) #> TRUE

# Data frames often have no actual dimension attribues:
has_dim(mtcars, how = "attr") #> FALSE
has_dimnames(mtcars, how = "attr") #> FALSE
has_rownames(mtcars, how = "row.names") #> TRUE (but have row.names one)

}
