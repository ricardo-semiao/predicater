% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predicates-attributes.R
\name{attributes-dimensions}
\alias{attributes-dimensions}
\alias{n_dims}
\alias{n_dimnames}
\alias{has_dimnames}
\alias{has_rownames}
\alias{has_dim}
\title{Attributes - Dimensions}
\usage{
n_dims(x, count_empty = TRUE, how = "dim")

n_dimnames(x, count_empty = TRUE, count_invalid = TRUE, how = "dimnames")

has_dimnames(x, n = NULL, how = "dimnames")

has_rownames(x, how = "dimnames")

has_dim(x, n = NULL, how = "dim")
}
\arguments{
\item{x}{[\code{any}] Object to check.}

\item{count_empty}{[\code{TRUE} | \code{FALSE}] Whether to count empty dimensions and
dimension names.}

\item{how}{[\code{character(1)}] How to extract the attribute:
\itemize{
\item For dimensions: \code{"dim"} for \code{\link[=dim]{dim()}} and \code{"attr"} for \code{\link[=attr]{attr()}}.
\item For dimension names: \code{"dimnames"} for \code{\link[=dimnames]{dimnames()}} and \code{"attr"} for
\code{\link[=attr]{attr()}}.
\item For rownames: \code{"rownames"} for \code{\link[=rownames]{rownames()}}, \code{"dimnames"} for the first
element of \code{\link[=dimnames]{dimnames()}}, and \code{"attr"} for \code{\link[=attr]{attr()}}.
}}

\item{count_invalid}{[\code{TRUE} | \code{FALSE}] Whether to dimension names that have
only non-NA or non-empty values.}

\item{n}{[\code{integer(1)}] Number of dimensions to check for. Set to \code{NULL} to
not test.}
}
\value{
\itemize{
\item [integer(1)] For \code{n_dims()} and \code{n_dimnames()}.
\item [\code{TRUE} | \code{FALSE}] For \code{has_dim()}, \code{has_dimnames()}, and \code{has_rownames()}.
}
}
\description{
Functions for the presence and number of dimensions and dimensions names of
an object.
\itemize{
\item \code{n_dims()} and \code{n_dimnames()} return the number of dimensions and dimension
names, respectively, controlling for empty dimensions and invalid names.
\item \code{has_dim()}, \code{has_dimnames()}, and \code{has_rownames()} return whether the
object has dimensions, dimension names, and rownames, respectively.
}
}
