% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-general_asserts.R
\name{test_multiple}
\alias{test_multiple}
\title{Validation - Test for one of multiple types}
\usage{
test_multiple(x, types, ...)
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{types}{[\code{character()}] A character vector of types to check against.}

\item{...}{[\code{list()} each] Lists of arguments to pass to the test function
for each type. The list names}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] for \code{test_multi()}.
\item [\verb{=x}] \code{invisible(x)} for \code{assert_multi()}, or aborts if the test fails.
}
}
\description{
This function checks if \code{x} passes at least one of a set of tests. \code{x} is
allowed to be any type \code{\link[=typeof]{typeof()}} in \code{types}, and needs to pass the
corresponding test for its type.

For example, if \code{x} is of type \code{"integer"}, then it is tested with
\code{\link[=test_integer]{test_integer()}}. Pass a list of arguments to the test function via \code{...},
with the same argument name as the type. E.g. \code{integer = list(len = 2)}.
}
\examples{
x <- list(a = 1:2, b = 3:4)

args <- list(
  types = c("integer", "list"),
  list = list(
    custom_map = \(elt) is_integer(elt, 1)
  )
  # Either a integer vector of a list of integer scalars
)

do.call(test_multiple, c(list(x), args)) #> FALSE (not all tests passed)

}
