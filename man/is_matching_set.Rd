% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks-data.R
\name{is_matching_set}
\alias{is_matching_set}
\title{Data checks - Check if object is in (and/or not in) a set}
\usage{
is_matching_set(x, yes = NULL, no = NULL, mode = "all")
}
\arguments{
\item{x}{[\code{atomic} | \code{list()}] An atomic vector or list to test.}

\item{yes, no}{[\code{atomic} | \code{list()} | \code{NULL}] A set of values that \code{x} should
be in, and not be in, respectively. If \code{NULL}, this check is ignored.}

\item{mode}{[\code{"all"} | \code{"any"} | \code{"only"}] The mode of the check: \code{"all"}
for all values in \code{x} must be in \code{yes}, \code{"any"} for at least one value in
\code{x} must be in \code{yes}, and \code{"only"} for all values in \code{x} must be in \code{yes}
and all values in \code{yes} must be in \code{x}.}
}
\value{
[\code{TRUE} | \code{FALSE}] The scalar result of the test.
}
\description{
This function checks if an object is in a set of values, and optionally if it
is not in another set of values. It can check if all, any, or only the values
are in the set.
}
\examples{
x <- c(5, 7, 5, 9, 9)

# Default mode checks if all values in `x` are in `yes` and not in `no`:
is_matching_set(x, yes = 1:10) #> TRUE
is_matching_set(x, yes = 1:8) #> FALSE
is_matching_set(x, yes = 1:10, no = 5) #> FALSE

# Mode `any` passes even if there are values in `x` that are not in `yes`:
is_matching_set(x, yes = 1:6, mode = "any") #> TRUE

# For mode `only`, all values in `x` must be in `yes` and vice versa:
is_matching_set(x, yes = 1:10, mode = "only") #> FALSE
is_matching_set(x, yes = c(5, 7, 9), mode = "only") #> TRUE

# `yes` can be NULL to test only `no` (independent of mode):
is_matching_set(x, no = 11) #> TRUE
is_matching_set(x) #> TRUE

}
