% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests-numeric.R
\name{tests-numeric}
\alias{tests-numeric}
\alias{test_integer}
\alias{assert_integer}
\alias{test_double}
\alias{assert_double}
\alias{test_complex}
\alias{assert_complex}
\title{Tests - Numeric vectors}
\usage{
test_integer(
  x,
  mode = "strict",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sorted = NULL,
  custom = NULL,
  sentinels = NULL,
  mode_tol = NULL
)

assert_integer(
  x,
  action = "abort",
  mode = "strict",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sorted = NULL,
  custom = NULL,
  sentinels = NULL,
  mode_tol = NULL,
  env = caller_env(),
  x_name = NULL,
  report_untested = TRUE,
  ...
)

test_double(
  x,
  mode = "double",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sentinels = c(null = "f", na = "f", nan = "f", inf = "f", t = "f", f = "f"),
  sorted = NULL,
  custom = NULL
)

assert_double(
  x,
  action = "abort",
  mode = "double",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sentinels = c(null = "f", na = "f", nan = "f", inf = "f", t = "f", f = "f"),
  sorted = NULL,
  custom = NULL,
  env = caller_env(),
  x_name = NULL,
  report_untested = TRUE,
  ...
)

test_complex(
  x,
  re_tests = NULL,
  im_tests = NULL,
  mod_tests = NULL,
  arg_tests = NULL,
  sentinels = NULL,
  custom = NULL
)

assert_complex(
  x,
  action = "abort",
  re_tests = NULL,
  im_tests = NULL,
  mod_tests = NULL,
  arg_tests = NULL,
  sentinels = NULL,
  custom = NULL,
  env = caller_env(),
  x_name = NULL,
  report_untested = TRUE,
  ...
)
}
\arguments{
\item{x}{[\code{any}] An  object to test.}

\item{mode, mode_tol}{[\code{"strict"} | \code{"range"} | \code{"range_tol"} | \code{"trunc"} |
\code{"trunc_tol"}, \code{double(1)}] The \code{mode} and \code{tol} arguments to pass to
\code{\link[=is_integer_like]{is_integer_like()}}.}

\item{len, na_n, dup_n, nan_n, inf_n}{}

\item{range}{[\code{integer()} | \code{NULL}] A vector with the upper and lower bound
for \code{x} values (\code{Inf} is allowed). Or a vector with three or more values to
test for \code{. \%in\% range}. Set to \code{NULL} to not test.}

\item{set}{[\code{integer()} | \code{list(yes = , no = )} | \code{NULL}] Test if all values
of \code{x} are in a set of allowed values. Use a list with \code{yes} and \code{no}
elements to defined allowed and disallowed values. Set to \code{NULL} to not test.}

\item{sorted}{[\code{"asc"} | \code{"desc"} | \code{NULL}] Test if \code{x} is sorted in ascending (\code{"asc"})   or descending (\code{"desc"}) order. Set to \code{NULL} to not test. Pair with \code{dup_n}   to test for strictly sorted values.}

\item{custom}{[\verb{function(x)} | \code{NULL}] A custom function that takes \code{x} as first argument   and returns a single \code{TRUE} or \code{FALSE}. Set to \code{NULL} to not test.}

\item{sentinels}{[\code{character()}] A named character that allows \code{x} to be some scalar sentinel instead. By default, none are allowed. Add each named entry below to change the behaviour:
\itemize{
\item \code{null}: \code{"f"} to disallow \code{NULL}, \code{"t"} to allow.
\item \code{na}: \code{"f"} to disallow \code{NA}, \code{"t"} to allow \code{NA}, \code{"any"} to allow any     NA value.
\item \code{nan}: \code{"f"} to disallow \code{NaN}, \code{"t"} to allow.
\item \code{inf}: \code{"f"} to disallow, \code{"+"} to allow \code{+Inf}, \code{"-"} to allow \code{-Inf},     \code{"+-"} to allow both.
\item \code{t}: \code{"f"} to disallow \code{TRUE}, \code{"t"} to allow.
\item \code{f}: \code{"f"} to disallow \code{FALSE}, \code{"t"} to allow.
}}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] for \verb{test_*()}.
\item [\verb{=x}] for \verb{assert_*()}, or aborts if the test fails.
}
}
\description{
Test if an object is an numeric vector:
\itemize{
\item \code{test_integer(x, mode = "strict")} tests for integer vectors.
\item \verb{test_integer_like(x, mode = *)}, tests for integer-like vectors via
\code{\link[=is_integer_like]{is_integer_like()}}, with the same modes as that function.
\item \code{test_double(x)} tests for double vectors.
}

They all are predicate tests, while the \verb{assert_*()} functions validate their
input, aborting if it fails the test.
}
