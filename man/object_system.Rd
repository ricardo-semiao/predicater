% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types-objects.R
\name{object_system}
\alias{object_system}
\alias{is_system}
\title{Types - Determine object-oriented system of object}
\usage{
object_system(x)

is_system(x, system)
}
\arguments{
\item{x}{[\code{any}] Object to check.}

\item{system}{[\code{character(1)}] Object-oriented system to check for.}
}
\value{
\itemize{
\item [\code{character(1)}] For \code{object_system()}: the object-oriented system of \code{x}.
\item [\code{TRUE} | \code{FALSE}] For \code{is_system()}: \code{TRUE} if \code{x} belongs to \code{system},
\code{FALSE} otherwise.
}
}
\description{
R has many object-oriented systems, including S3, S4, RC, R6, R.oo, S7,
proto, and ggproto. This function determines to which of these an object
belongs, extending the test done by \code{\link[sloop:otype]{sloop::otype()}}.

If \code{x} fails \code{\link[=is.object]{is.object()}}, the function returns \code{"base"}. \code{x} matches \code{"S3"}
if it is an object but does not match any other system, so systems not
implemented here will fall into that category.

\code{is_system()} tests if an object belongs to a specific system.
}
\details{
The implemented check is as below:
\enumerate{
\item If not \code{\link[=is.object]{is.object()}}, return \code{"base"}.
\item Else, if \code{\link[=isS4]{isS4()}}, return \code{"RC"} if \code{is(x, "refClass")}, else return
\code{"S4"}.
\item Else, if \code{inherits_any(x, "S7_object")} and \code{typeof(x) == "object"} (as an
additional safeguard), return \code{"S7"}.
\item Else, if \code{inherits_any(x, "R6")} and \code{\link[rlang:is_environment]{rlang::is_environment()}}, return
\code{"R6"}.
\item Else, if \code{inherits_any(x, "ggproto")} and \code{\link[rlang:is_environment]{rlang::is_environment()}},
\item return
\code{"ggproto"}.
\item Else, if \code{inherits_any(x, "proto")} and \code{all(c(".super", ".that") \%in\% names(x))} and \code{\link[rlang:is_environment]{rlang::is_environment()}}, return \code{"proto"}.
\item Else, if \code{inherits_any(x, "Object")} and \code{all(c(".env", "...modifiers", "...finalize") \%in\% names(attributes(x)))} and \code{is_logical(x, 1)}, return
\code{"R.oo"}.
}
}
\examples{
object_system(1:10) #> "base"

object_system(factor(letters)) #> "S3"

track <- setClass("track", slots = c(x = "numeric", y = "numeric"))
xs4 <- track(x = 1:10, y = 1:10 + rnorm(10))
object_system(xs4) #> "S4"

}
