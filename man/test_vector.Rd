% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-collections.R
\name{test_vector}
\alias{test_vector}
\alias{assert_vector}
\title{Validation - General vectors}
\usage{
test_vector(
  x,
  mode = "atomic",
  len = NULL,
  n_na = NULL,
  n_null = NULL,
  n_empty = NULL,
  n_dup = NULL,
  sentinels = NULL,
  custom = NULL,
  custom_map = NULL
)

assert_vector(
  x,
  mode = "atomic",
  len = NULL,
  n_na = NULL,
  n_null = NULL,
  n_empty = NULL,
  n_dup = NULL,
  sentinels = NULL,
  custom = NULL,
  custom_map = NULL,
  action = "abort",
  env = caller_env(),
  x_name = NULL,
  short_circuit = TRUE,
  report_untested = TRUE,
  args_cnd = list()
)
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{mode}{[\code{"atomic"} | \code{"list"} | \code{"expression"} | \code{"pairlist"}]
Allowed vector types. They are additive: \code{"atomic"} allows atomic vectors,
\code{list} allows atomic and lists, \code{expression} allows atomic, lists, and
expression objects, and \code{pairlist} allows all.}

\item{len, n_na, n_null, n_empty, n_dup}{[\code{numeric()} | \verb{\\()\{\}} | \code{NULL}] Possible values for the length, number of \code{NA} elements, number of \code{NULL} elements, number of elements with zero length, number of duplicate elements. The   options of each argument 'arg' are:
\itemize{
\item \code{NULL} to not test.
\item A single non-negative number to test for \code{. == arg}. If \code{Inf},     \code{. == length(x)}.
\item A single negative number to test for \code{. == length(x) + arg}.
\item A vector of two non-negative numbers to test for \verb{arg[1] <= . <= arg[2]} (\code{Inf} is allowed).
\item A vector of three or more non-negative numbers to test for \code{. \%in\% arg}.
\item A function that recieves the value to test and the length of \code{x}, and
returns a single \code{TRUE} or \code{FALSE}.
}}

\item{sentinels}{[\code{character()} | \code{NULL}] Each entry in this character vector allows \code{x} to   also be some scalar sentinel below. Set to \code{NULL} to disconsider sentinels.
\itemize{
\item \code{"null"} for \code{NULL}.
\item \code{"empty"} for any zero-length object.
\item \code{"na"} for any \code{NA} type, or \code{"na_logical"} for \code{NA}, \code{"na_integer"} for     \code{NA_integer_}, \code{"na_real"} for \code{NA_real_}, \code{"na_complex"} for     \code{NA_complex_}, and \code{"na_character"} for \code{NA_character_}.
\item \code{"nan"} for \code{NaN}.
\item \code{"+inf"} for \code{+Inf}, \code{"-inf"} for \code{-Inf}, and \code{"inf"} for both.
\item \code{"true"}/\code{"t"} for \code{TRUE}, and \code{"false"}/\code{"f"} for \code{FALSE}.
}}

\item{custom}{[\verb{function(x)} | \code{NULL}] A custom function that takes \code{x} as first argument   and returns a single \code{TRUE} or \code{FALSE}. Set to \code{NULL} to not test.}

\item{custom_map}{[\verb{function(x)} | \code{NULL}] A custom function that is applied to each element   of \code{x}. Must return a single \code{TRUE} or \code{FALSE} for each element. Set to   \code{NULL} to not test.}

\item{action}{[\code{"abort"} | \code{"warn"} | \code{"inform"}] Action to take when the test fails:
\itemize{
\item \code{"abort"} to stop execution and throw an error.
\item \code{"warning"} to issue a warning and return \code{invisible(x)}.
\item \code{"message"} to issue a message and return \code{invisible(x)}.
}}

\item{env}{[\code{environment()} | \code{call()} | \code{NULL} | \code{missing_arg()}] The call to inform as   the origin of the error, passed to \code{\link[rlang:abort]{rlang::abort()}}:
\itemize{
\item An environment in the call stack or a hard-coded defused call.
\item \code{NULL} for no information.
\item \code{missing_arg()} to use the assert function itself.
\item The default is \code{caller_env()}, to display the function where the assertion     was called.
}}

\item{x_name}{[\code{character(1)} | \code{NULL}] The name of the object to use in the error message.   If \code{NULL}, the name is inferred from the expression passed to \code{x}.}

\item{short_circuit}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, the tests results will be reported up to the   first failure. Else, all tests results are reported. The former is more   efficient, while the latter is more informative.}

\item{report_untested}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, the tests that were not run due to short-  circuiting will be reported as untested, else, ignored.}

\item{args_cnd}{[\code{list()}] Additional arguments passed to \code{\link[cli:cli_abort]{cli::cli_abort()}}, \code{\link[cli:cli_abort]{cli::cli_warn()}},   or \code{\link[cli:cli_abort]{cli::cli_inform()}}, based on the chosen \code{action}.}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] for \code{test_vector()}.
\item [\verb{=x}] \code{invisible(x)} for \code{assert_vector()}, or aborts if the test fails.
}
}
\description{
Test if an input is a vector of a given mode (atomic, list, expression, or
pairlist) and optionally validate length, missing values, duplicate counts,
set inclusion, and ordering constraints.

\code{test_vector()} is the predicate test, while \code{assert_vector()} validates
its input, aborting if it fails the test.
}
\examples{
x <- list(1, 2, NA, 2, NULL)

args <- list(
  mode = "list",          # Allow atomic vectors and lists (will pass)
  len = c(1, 10),         # Length must be between 1 and 10 (will pass)
  n_na = 0,               # No NA values allowed (will fail)
  n_null = c(0, 1),       # At most 1 NULL element allowed (will pass)
  n_empty = 0,            # No empty elements allowed (will pass)
  n_dup = 0,              # No duplicate elements allowed (will fail)
  sentinels = c("null"),  # Allow NULL x (not the case of x)
  custom = NULL,          # Not tested
  custom_map = \(elt) length(elt) <= 1
  # All list elements must have length <= 1 (will pass)
)

do.call(test_vector, c(list(x), args)) #> FALSE (not all tests passed)

try(do.call(assert_vector, c(list(x), args, short_circuit = FALSE))) #> Error

}
