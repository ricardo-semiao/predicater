% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare-match.R
\name{match_hash}
\alias{match_hash}
\alias{match_id}
\alias{match_ptype}
\alias{match_when}
\title{Compare - Pattern matching}
\usage{
match_hash(x, ..., nomatch = NULL, htab = NULL, type = NULL, size = NULL)

match_id(x, ..., nomatch = NULL, fun = "identical", args_id = list())

match_ptype(x, ..., nomatch = NULL, args_ptype = list())

match_when(x, ..., nomatch = NULL)
}
\arguments{
\item{x}{[\code{any}] Object to match.}

\item{...}{[\verb{<formula>} each]
Cases to match. Each case is a formula with
the left-hand side being the value to match against and the right-hand side
being the value to return if the case matches. Cases are evaluated in
order, and in the caller environment.}

\item{nomatch}{[\code{any}] Value to return if no cases match. Use \code{stop()} or
similar to err.}

\item{htab, type, size}{[\code{hashtab()} | NULL, \code{character(1)} | \code{NULL},
\code{integer(1)} | \code{NULL}]
For \code{match_hash}: an existing hash table, or the arguments passed to
\code{\link[utils:hashtab]{utils::hashtab()}} to create a new one.}

\item{fun, args_id}{[\code{character(1)}, \code{list()}] For \code{match_id}: which
'identical' function to use, \code{"identical"} for \code{\link[=identical]{identical()}} or
\code{"identical2"} for \code{\link[=identical2]{identical2()}}; and a list of arguments to pass to it.}

\item{args_ptype}{[\code{list()}] For
\code{match_ptype}. A list of arguments to pass to \code{\link[=is_ptype]{is_ptype()}}.}
}
\value{
[\code{any}] The value of the first matching case, or \code{nomatch} if no
cases match.
}
\description{
Pattern matching functions to return a value based on a case of \code{x}. Similar
to \code{\link[=switch]{switch()}} but allows for any type for object as the case. Cases are two
sided formulas with the LHS being the case and the RHS being the return
value.
\itemize{
\item For \code{match_hash} and \code{match_id}, cases are any R object to directly compare
with \code{x}.
\item For \code{match_ptype}, cases are prototypes to check if \code{x} \code{\link[=is_ptype]{is_ptype()}}.
\item For \code{match_when}, cases are any predicate expression, that matches when it
evaluates to \code{TRUE}.
}
}
\details{
The return value is evaluated dynamically and can refer to \code{x}, except for
\code{match_hash}, which in turn is faster and can be supplied a pre-built hash
table.
}
\examples{
match_hash(1.42, 1.42 ~ base::sum, "oi" ~ -Inf) #> base::sum

# The htab can be reused to speed up repeated calls to match_hash():
htab <- utils::hashtab(size = 2)
utils::sethash(htab, 1.42, base::sum)
utils::sethash(htab, "oi", -Inf)
match_hash(1.42, htab = htab) #> base::sum

match_id(1.42, "oi" ~ -Inf, 1.42 ~ .x * 2) #> 2.84

match_ptype(1.42, character() ~ "chr", double() ~ "dbl") #> "dbl"

match_when("oi", grepl("1", .x) ~ "1", grepl("o", .x) ~ "o") #> "o"

}
