% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-other_types.R
\name{test_function}
\alias{test_function}
\alias{assert_function}
\title{Validation - Function}
\usage{
test_function(
  x,
  mode = "function",
  args_names = NULL,
  fn_env = NULL,
  dots = NULL,
  generic = NULL,
  method = NULL,
  sentinels = NULL,
  custom = NULL
)

assert_function(
  x,
  mode = "function",
  args_names = NULL,
  fn_env = NULL,
  dots = NULL,
  generic = NULL,
  method = NULL,
  sentinels = NULL,
  custom = NULL,
  action = "abort",
  env = caller_env(),
  x_name = NULL,
  short_circuit = TRUE,
  report_untested = TRUE,
  args_cnd = list()
)
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{mode}{[\code{"function"} | \code{"closure"} | \code{"builtin"} | \code{"special"} |
\code{"primitive"} | \code{NULL}]
Expected function type(s) out of \code{"function"} (any function), \code{"closure"},
\code{"builtin"}, \code{"special"}, or \code{"primitive"} (any of the former two). Set to
\code{NULL} to not test.}

\item{args_names}{[\code{character()} | \code{NULL}] Expected exact argument names of
the function. Set to \code{NULL} to not test.}

\item{fn_env}{[\code{environment} | \code{NULL}] Expected environment of the function.
Checks if \code{fn_env(x)} is identical to \code{env} or inherits from it via
\code{\link[rlang:env_inherits]{rlang::env_inherits()}}. Set to \code{NULL} to not test.}

\item{dots}{[\code{TRUE} | \code{FALSE} | \code{NULL}] Test if the function has \code{...} in
its arguments. Set to \code{NULL} to not test.}

\item{generic, method}{[\code{TRUE} | \code{FALSE} | \code{NULL} each] Test if the function
is an S3 generic or method via
\code{\link[sloop:is_s3_generic]{sloop::is_s3_generic()}}/\code{\link[sloop:is_s3_generic]{sloop::is_s3_method()}}. Set to \code{NULL} to not
test.}

\item{sentinels}{[\code{character()} | \code{NULL}] Each entry in this character vector allows \code{x} to   also be some scalar sentinel below. Set to \code{NULL} to disconsider sentinels.
\itemize{
\item \code{"null"} for \code{NULL}.
\item \code{"empty"} for any zero-length object.
\item \code{"na"} for any \code{NA} type, or \code{"na_logical"} for \code{NA}, \code{"na_integer"} for     \code{NA_integer_}, \code{"na_real"} for \code{NA_real_}, \code{"na_complex"} for     \code{NA_complex_}, and \code{"na_character"} for \code{NA_character_}.
\item \code{"nan"} for \code{NaN}.
\item \code{"+inf"} for \code{+Inf}, \code{"-inf"} for \code{-Inf}, and \code{"inf"} for both.
\item \code{"true"}/\code{"t"} for \code{TRUE}, and \code{"false"}/\code{"f"} for \code{FALSE}.
}}

\item{custom}{[\verb{function(x)} | \code{NULL}] A custom function that takes \code{x} as first argument   and returns a single \code{TRUE} or \code{FALSE}. Set to \code{NULL} to not test.}

\item{action}{[\code{"abort"} | \code{"warn"} | \code{"inform"}] Action to take when the test fails:
\itemize{
\item \code{"abort"} to stop execution and throw an error.
\item \code{"warning"} to issue a warning and return \code{invisible(x)}.
\item \code{"message"} to issue a message and return \code{invisible(x)}.
}}

\item{env}{[\code{environment()} | \code{call()} | \code{NULL} | \code{missing_arg()}] The call to inform as   the origin of the error, passed to \code{\link[rlang:abort]{rlang::abort()}}:
\itemize{
\item An environment in the call stack or a hard-coded defused call.
\item \code{NULL} for no information.
\item \code{missing_arg()} to use the assert function itself.
\item The default is \code{caller_env()}, to display the function where the assertion     was called.
}}

\item{x_name}{[\code{character(1)} | \code{NULL}] The name of the object to use in the error message.   If \code{NULL}, the name is inferred from the expression passed to \code{x}.}

\item{short_circuit}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, the tests results will be reported up to the   first failure. Else, all tests results are reported. The former is more   efficient, while the latter is more informative.}

\item{report_untested}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, the tests that were not run due to short-  circuiting will be reported as untested, else, ignored.}

\item{args_cnd}{[\code{list()}] Additional arguments passed to \code{\link[cli:cli_abort]{cli::cli_abort()}}, \code{\link[cli:cli_abort]{cli::cli_warn()}},   or \code{\link[cli:cli_abort]{cli::cli_inform()}}, based on the chosen \code{action}.}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] for \code{test_function()}.
\item [\verb{=x}] \code{invisible(x)} for \code{assert_function()}, or aborts if the test fails.
}
}
\description{
Test if an object is a function, with options to inspect function type, argument
names, environments, S3 generics, and methods.
}
\examples{
x <- function(a, b = 1, ...) {
  a + b
}

args <- list(
  mode = "closure",            # Must be a standard closure function (will pass)
  args_names = c("a", "b"),    # Exact argument names must match (will fail)
  fn_env = rlang::global_env(),
  # Function environment must equal or inherit from global env (will pass)
  dots = TRUE,                 # Function must accept `...` in arguments (will pass)
  generic = FALSE,             # Must not be an S3 generic (will pass)
  method = FALSE,              # Must not be an S3 method (will pass)
  sentinels = c("null"),       # Allow NULL function (not the case of x)
  custom = NULL                # No custom predicate
)

do.call(test_function, c(list(x), args)) #> FALSE (not all tests passed)

try(do.call(assert_function, c(list(x), args, short_circuit = FALSE))) #> Error

}
