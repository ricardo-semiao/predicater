% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks-types-other_types.R
\name{predicates-collections}
\alias{predicates-collections}
\alias{is_list}
\alias{is_pairlist2}
\alias{is_environment2}
\alias{is_vector}
\alias{is_collection}
\title{Type checks - Collection types}
\usage{
is_list(x, n = NULL)

is_pairlist2(x, n = NULL)

is_environment2(x, n = NULL)

is_vector(x, n = NULL)

is_collection(
  x,
  n = NULL,
  expr = TRUE,
  pairlist = TRUE,
  env = TRUE,
  null = FALSE,
  dots = FALSE
)
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{n}{[\code{integer(1)} | \code{NULL}] Length of \code{x}, set to \code{NULL} to not test.}

\item{expr, pairlist, env, null, dots}{[\code{TRUE} | \code{FALSE}] Whether to include
expression, pairlist, environment, \code{NILL}, or the \code{...} objects as
collections.}
}
\value{
[\code{TRUE} | \code{FALSE}] The scalar result of the test.
}
\description{
In R, there are several types (\code{\link[=typeof]{typeof()}}) that can "store elements":
\itemize{
\item The atomic vector types (see \link{predicates-atomic}).
\item \code{"list"}: the generic vector type (can store elements of any type). Tested
by \code{\link[rlang:type-predicates]{rlang::is_list()}}.
\item \code{"pairlist"}: a linked list, used in R's internals. Tested by
\code{is_pairlist2()}.
\item \code{"environment"}: a hash table, used to store variables and their values.
Tested by \code{is_environment2()}.
\item \code{"expression"}: a vector of unevaluated R code objects. Tested by
\code{\link[=is_expression2]{is_expression2()}}.
}

See the 'Details' section for what behaviour you can expect from these types.

Additionally:
\itemize{
\item \code{\link[rlang:type-predicates]{rlang::is_atomic()}} tests for any atomic vector type.
\item \code{\link[rlang:type-predicates]{rlang::is_vector()}} tests for atomic or generic (list) vectors.
\item \code{is_collection()} tests for any of the above collection types, with options
to exclude any of them.
}
}
\details{
All collection types:
\itemize{
\item Have varying \code{\link[=length]{length()}}.
\item Can have \code{\link[=names]{names()}}.
\item Can have their elements accessed by names (if present) via \code{[}, \code{[[}, and \code{$}.
\item All but environment can have their elements accessed by integer indexes
too, and their names set via \code{names(x) <- value}.
}

Note that the result of acessing out-of-bounds indexes depends on the type and
operator, and is very quirky and inconsistent across R. See \href{https://adv-r.hadley.nz/subsetting.html}{Advanced R, 2nd edition, chapter  4} for more
details.

Objects that are not considered collections:
\itemize{
\item Objects of type \code{"s4"} or \code{"object"} are a 'collection of slots', but their
data is stored in their attributes, which any R object can do, and their
length is always 1.
\item The \code{"dots"} object is a collection of promises, have varying length,
names, etc. But it is low-level, and removed from this definition for
simplicity.
\item 'NULL' (and 'any') have length 0, and are often thought as empty collections,
but in face of \code{integer(0)} and \code{list()}, such thought is not the best.
\item 'language' and 'promise' can have names, but don't have varying length.
\item All the other types have lenght 1, and cannot have names.
}

See \code{\link[rlang:is_namespace]{rlang::is_namespace()}} for another environment-related test.
}
\examples{
is_collection(list(1, 2, 3)) #> TRUE
is_collection(rlang::env(a = 1, b = 2), n = 2) #> TRUE

# NULL is not considered a collection:
is_collection(NULL) #> FALSE
is_collection(NULL, null = TRUE) #> TRUE

# This is the difference between rlang::is_empty() and predicater::is_empty2():
rlang::is_empty(NULL) #> TRUE
try(is_empty2(NULL)) #> Error

}
