% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests-numeric.R
\name{test-numeric}
\alias{test-numeric}
\alias{test_integer}
\alias{test_double}
\alias{test_complex}
\alias{assert_integer}
\alias{assert_double}
\alias{assert_complex}
\title{Tests - Numeric vectors}
\usage{
test_integer(
  x,
  mode = "strict",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sorted = NULL,
  custom = NULL,
  custom_map = NULL,
  sentinels = NULL,
  mode_tol = NULL
)

test_double(
  x,
  mode = "double",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sorted = NULL,
  sentinels = NULL,
  custom = NULL,
  custom_map = NULL
)

test_complex(
  x,
  re_tests = NULL,
  im_tests = NULL,
  mod_tests = NULL,
  arg_tests = NULL,
  sentinels = NULL,
  custom = NULL,
  custom_map = NULL
)

assert_integer(
  x,
  mode = "strict",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sorted = NULL,
  custom = NULL,
  custom_map = NULL,
  sentinels = NULL,
  mode_tol = NULL,
  action = "abort",
  env = caller_env(),
  x_name = NULL,
  short_circuit = TRUE,
  report_untested = TRUE,
  args_cnd = list()
)

assert_double(
  x,
  mode = "double",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sorted = NULL,
  sentinels = NULL,
  custom = NULL,
  custom_map = NULL,
  action = "abort",
  env = caller_env(),
  x_name = NULL,
  short_circuit = TRUE,
  report_untested = TRUE,
  args_cnd = list()
)

assert_complex(
  x,
  re_tests = NULL,
  im_tests = NULL,
  mod_tests = NULL,
  arg_tests = NULL,
  sentinels = NULL,
  custom = NULL,
  custom_map = NULL,
  action = "abort",
  env = caller_env(),
  x_name = NULL,
  short_circuit = TRUE,
  report_untested = TRUE,
  args_cnd = list()
)
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{mode, mode_tol}{[\code{"strict"} | \code{"range"} | \code{"range_tol"} | \code{"trunc"} |
\code{"trunc_tol"}, \code{double(1)}] For \verb{*_integer()}: the \code{mode} and \code{tol}
arguments to pass to \code{\link[=is_integer_like]{is_integer_like()}}.}

\item{len, na_n, dup_n, nan_n, inf_n}{[\code{numeric()} | \verb{\\()\{\}} | \code{NULL}] Possible values for the length, number of \code{NA} elements, number of duplicate elements, number of \code{NaN} elements, number of \code{Inf} elements. The options of each argument 'arg' are:
\itemize{
\item \code{NULL} to not test.
\item A single non-negative number to test for \code{. == arg}. If \code{Inf},     \code{. == length(x)}.
\item A single negative number to test for \code{. == length(x) + arg}.
\item A vector of two non-negative numbers to test for \verb{arg[1] <= . <= arg[2]} (\code{Inf} is allowed).
\item A vector of three or more non-negative numbers to test for \code{. \%in\% arg}.
\item A function that recieves the value to test and the length of \code{x}, and
returns a single \code{TRUE} or \code{FALSE}.
}}

\item{range}{[\code{integer()} | \code{NULL}] A vector with the upper and lower bound
for \code{x} values (\code{Inf} is allowed). Or a vector with three or more values to
test for \code{. \%in\% range}. Set to \code{NULL} to not test.}

\item{set}{[\code{integer()} | \code{list(yes = , no = , mode = )} | \code{NULL}] Test if all values   of \code{x} are in a set of allowed values. Use a list with \code{yes} and/or \code{no} elements to defined allowed and disallowed values, with \code{mode}s \code{"all"} (all \code{x} in \code{yes}, the default), \code{"only"} (all and only \code{x} in \code{yes}), or \code{"any"} (any \code{x} in \code{yes}). Set to \code{NULL} to not test.}

\item{sorted}{[\code{"asc"} | \code{"desc"} | \code{NULL}] Test if \code{x} is sorted in ascending (\code{"asc"})   or descending (\code{"desc"}) order. Set to \code{NULL} to not test. Pair with \code{dup_n}   to test for strictly sorted values.}

\item{custom}{[\verb{function(x)} | \code{NULL}] A custom function that takes \code{x} as first argument   and returns a single \code{TRUE} or \code{FALSE}. Set to \code{NULL} to not test.}

\item{custom_map}{[\verb{function(x)} | \code{NULL}] A custom function that is applied to each element   of \code{x}. Must return a single \code{TRUE} or \code{FALSE} for each element. Set to   \code{NULL} to not test.}

\item{sentinels}{[\code{character()} | \code{NULL}] Each entry in this character vector allows \code{x} to also be some scalar sentinel below. Set to \code{NULL} to disconsider sentinels.
\itemize{
\item \code{"null"} for \code{NULL}.
\item \code{"empty"} for any zero-length object.
\item \code{"na"} for any \code{NA} type, or \code{"na_logical"} for \code{NA}, \code{"na_integer"} for     \code{NA_integer_}, \code{"na_real"} for \code{NA_real_}, \code{"na_complex"} for     \code{NA_complex_}, and \code{"na_character"} for \code{NA_character_}.
\item \code{"nan"} for \code{NaN}.
\item \code{"+inf"} for \code{+Inf}, \code{"-inf"} for \code{-Inf}, and \code{"inf"} for both.
\item \code{"t"} for \code{TRUE}, and \code{"f"} for \code{FALSE}.
}}

\item{re_tests, im_tests, mod_tests, arg_tests}{[\code{list()} | \code{NULL}] For
\verb{*_complex()}: a list with the same named arguments as \code{test_double()}, to
test the real, imaginary, modulus and argument values of \code{x}.}

\item{action}{[\code{"abort"} | \code{"warn"} | \code{"inform"}] Action to take when the test fails:
\itemize{
\item \code{"abort"} to stop execution and throw an error.
\item \code{"warning"} to issue a warning and return \code{invisible(x)}.
\item \code{"message"} to issue a message and return \code{invisible(x)}.
}}

\item{env}{[\code{environment()} | \code{call()} | \code{NULL} | \code{missing_arg()}] The call to inform as the origin of the error, passed to \code{\link[rlang:abort]{rlang::abort()}}:
\itemize{
\item An environment in the call stack or a hard-coded defused call.
\item \code{NULL} for no information.
\item \code{missing_arg()} to use the assert function itself.
\item The default is \code{caller_env()}, to display the function where the assertion     was called.
}}

\item{x_name}{[\code{character(1)} | \code{NULL}] The name of the object to use in the error message.   If \code{NULL}, the name is inferred from the expression passed to \code{x}.}

\item{short_circuit}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, the tests results will be reported up to the   first failure. Else, all tests results are reported. The former is more   efficient, while the latter is more informative.}

\item{report_untested}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, the tests that were not run due to short-  circuiting will be reported as untested, else, ignored.}

\item{args_cnd}{[\code{list()}] Additional arguments passed to \code{\link[cli:cli_abort]{cli::cli_abort()}}, \code{\link[cli:cli_abort]{cli::cli_warn()}},   or \code{\link[cli:cli_abort]{cli::cli_inform()}}, based on the chosen \code{action}.}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] for \verb{test_*()}.
\item [\verb{=x}] \code{invisible(x)} for \verb{assert_*()}, or aborts if the test fails.
}
}
\description{
Test if an object is a numeric vector:
\itemize{
\item \code{test_integer(x, mode = "strict")} tests for integer vectors.
\item \verb{test_integer_like(x, mode = *)} tests for integer-like vectors via
\code{\link[=is_integer_like]{is_integer_like()}}, with its multiple modes.
\item \code{test_double(x, mode = "double")} tests for double vectors.
\item \code{test_double(x, mode = "numeric")} tests for double or integer vectors.
\item \code{test_complex()} tests for complex vectors, delegating tests on its real,
imaginary, modulus, and argument parts, to \code{test_double()}.
}

They all are predicate tests, while the \verb{assert_*()} functions validate their
input, aborting if it fails the test.
}
