% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests-atomic.R
\name{tests-integer}
\alias{tests-integer}
\alias{test_integer}
\alias{assert_integer}
\title{Tests - Integers}
\usage{
test_integer(
  x,
  mode = "strict",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sentinels = c(null = "f", na = "f", nan = "f", inf = "f", t = "f", f = "f"),
  sorted = NULL,
  custom = NULL,
  mode_tol = sqrt(.Machine$double.eps)
)

assert_integer(
  x,
  mode = "strict",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sentinels = c(null = "f", na = "f", nan = "f", inf = "f", t = "f", f = "f"),
  sorted = NULL,
  custom = NULL,
  mode_tol = sqrt(.Machine$double.eps),
  env = caller_env(),
  x_name = NULL,
  raise = TRUE
)
}
\arguments{
\item{x}{[\code{any}] An  object to test.}

\item{mode, mode_tol}{[\code{"strict"} | \code{"range"} | \code{"range_tol"} | \code{"trunc"} |
\code{"trunc_tol"}, \code{double(1)}] The \code{mode} and \code{tol} arguments to pass to
\code{\link[=is_integer_like]{is_integer_like()}}.}

\item{len, na_n, dup_n, nan_n, inf_n}{[\code{integer(1)} | \code{integer(2)} | \verb{\\()\{\}} |
\code{NULL}] Possible values for the length, data values, number of \code{NA} values,
and number of duplicates. \code{nan_n} and \code{inf_n} are only relevant if \code{mode}
is \code{"trunc"} or \code{"trunc_tol"}. The options are:
\itemize{
\item \code{NULL} to not test.
\item A single non-negative number to test for \code{. == range}.
\item A single negative number to test for \code{. == length(x) + range}.
\item A vector of two non-negative numbers to test for \verb{range[1] <= . <= range[2]}. \code{Inf} is allowed.
\item A function that recieves the value to test (e.g. the number of NAs) and
the length of \code{x}, and returns a single \code{TRUE} or \code{FALSE}.
}}

\item{range}{[\code{integer(2)} | \code{NULL}] A vector with the upper and lower bound
for \code{x} values. \code{Inf} is allowed. Set to \code{NULL} to not test.}

\item{set}{[\code{integer()} | \code{list(yes = , no = )} | \code{NULL}] Test if all values
of \code{x} are in a set of allowed values. Use a list with \code{yes} and \code{no}
\code{integer()} elements to defined allowed and disallowed values. Set to
\code{NULL} to not test.}

\item{sentinels}{[\code{character()}] A named character that allows \code{x} to be
some scalar sentinel instead. By default, none are allowed. Add each named
entry to change the behaviour:
\itemize{
\item \code{null} (\code{NULL}): \code{"f"} to disallow, \code{"t"} to allow.
\item \code{na} (\code{NA}): \code{"f"} to disallow, \code{"t"} to allow \code{NA_integer_}, \code{"any"} to
allow any NA value.
\item \code{nan} (\code{NaN}): \code{"f"} to disallow, \code{"t"} to allow.
\item \code{inf} (\code{Inf}): \code{"f"} to disallow, \code{"+"} to allow \code{+Inf}, \code{"-"} to allow
\code{-Inf}, \code{"+-"} to allow both.
\item \code{t} (\code{TRUE}): \code{"f"} to disallow, \code{"t"} to allow.
\item \code{f} (\code{FALSE}): \code{"f"} to disallow, \code{"t"} to allow.
}}

\item{sorted}{[\code{"asc"} | \code{"desc"} | \code{NULL}] Test if \code{x} is sorted in
ascending (\code{"asc"}) or descending (\code{"desc"}) order. Set to \code{NULL} to not
test. Pair with \code{dup_n} to test for strictly sorted values.}

\item{custom}{[\verb{\\()\{\}} | \code{NULL}] A custom function that takes \code{x} as first
argument and returns a single \code{TRUE} or \code{FALSE}. Set to \code{NULL} to not test.}
}
\value{
[\code{logical(1)}, \verb{=x} | \code{character()}]
}
\description{
Test if an object is an integer vector. The \code{mode} arguments allow to
consider only objects of \code{\link[=typeof]{typeof()}} \code{"integer"} (\code{mode = "strict"}), or also
double objects that have integer values, by passing \code{mode} and \code{mode_args} to
\code{\link[=is_integer_like]{is_integer_like()}} (see its help page for more details).
}
