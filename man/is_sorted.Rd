% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks-data.R
\name{is_sorted}
\alias{is_sorted}
\title{Data checks - Check if object is ordered}
\usage{
is_sorted(x, order = "asc", na.rm = FALSE, strictly = FALSE)
}
\arguments{
\item{x}{[\code{atomic}] An atomic vector to test.}

\item{order}{[\code{"asc"} | \code{"desc"}] The order to check for: \code{"asc"} for
ascending, \code{"desc"} for descending.}

\item{na.rm}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, \code{NA} values are removed before
checking for order. If \code{FALSE}, \code{NA} values propagate and imply in an \code{NA}
result.}

\item{strictly}{[\code{TRUE} | \code{FALSE}] If \code{TRUE}, the function checks for strict
order, meaning that no two elements can be equal.}
}
\value{
[\code{TRUE} | \code{FALSE} | \code{NA}] The scalar result of the test.. If \code{na.rm = TRUE}, the result is
always \code{TRUE} or \code{FALSE}.
}
\description{
This function is a wrapper around \code{\link[=is.unsorted]{is.unsorted()}} to check if a vector is
sorted in ascending or descending order.
}
\examples{
# Control the order of the check with `order` argument
is_sorted(1:5) #> TRUE
is_sorted(1:5, order = "desc") #> FALSE

# Control the handling of NA values with `na.rm` argument:
is_sorted(c(1, 2, NA, 4), na.rm = TRUE) #> TRUE

# Control the strictness of the order with `strictly` argument:
is_sorted(c(1, 2, 2, 4), strictly = TRUE) #> FALSE

}
