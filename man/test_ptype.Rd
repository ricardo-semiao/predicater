% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert.R
\name{test_ptype}
\alias{test_ptype}
\title{Test if object is of a given prototype}
\usage{
test_ptype(
  x,
  ptype,
  ...,
  msg = NULL,
  env = caller_env(),
  x_name = NULL,
  cnd_fun = cli_abort,
  cnd_args = list()
)
}
\arguments{
\item{x}{[\code{any}] The object to check.}

\item{ptype}{[\code{any}] The prototype to check against. See \code{\link[=is_ptype]{is_ptype()}} for
details on what is a prototype.}

\item{...}{Additional arguments to pass to \code{\link[=is_ptype]{is_ptype()}}.}

\item{msg}{[\code{character(1)} | \code{NULL}] The message to use if the check fails.
If \code{NULL}, a default message is generated.}

\item{env}{[\code{environment()}] The environment to use for the condition. Often
useful to remove this helper form the trace stack.}

\item{x_name}{[\code{character(1)} | \code{NULL}] The name of \code{x} to print in
messages. In \code{NULL}, the name is inferred from \code{x}'s symbol, if possible.}

\item{cnd_fun}{[\verb{function()}] The function to use to generate the condition.
Defaults to \code{cli_abort()}.}

\item{cnd_args}{[\code{list()}] Additional arguments to pass to \code{cnd_fun}.}
}
\value{
[\code{TRUE}] Invisibly \code{TRUE}, or aborts if the check fails.
}
\description{
This function wraps \code{\link[=is_ptype]{is_ptype()}} and aborts with a custom message if the
check fails. It is useful for testing function arguments.
}
\examples{
try({
  f <- \(x) test_ptype(x, numeric())
  f("a")
})
#> Error in `f()`:
#> ! Argument `x` is not of prototype `numeric()`.

}
