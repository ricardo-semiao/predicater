% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types-numeric.R
\name{predicates-infinite}
\alias{predicates-infinite}
\alias{are_finite}
\alias{is_finite}
\alias{are_nan}
\alias{is_nan}
\alias{are_inf}
\alias{is_inf}
\title{Predicates - Inf and NaN}
\usage{
are_finite(x, na = "na")

is_finite(x, n = NULL, na = "na")

are_nan(x, na = "na")

is_nan(x, n = NULL, na = "na")

are_inf(x, na = "na", signs = "+-")

is_inf(x, n = NULL, na = "na", signs = "+-")
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{na}{[\code{character(1)}] How to treat \code{NA} values: \code{"f"}
to return \code{FALSE}, \code{"t"} return \code{TRUE}, or \code{"na"} to return \code{NA}.}

\item{n}{[\code{integer(1)} | \code{NULL}] Length of \code{x}, set to \code{NULL} to not test.}

\item{signs}{[\code{character(1)}] For \code{are_inf()} and \code{is_inf()} -- which signs
of infinity to allow: \code{"+"} for positive infinity, \code{"-"} for negative
infinity, or \code{"+-"} for both.}
}
\value{
[\code{logical(length(x))}, \code{logical(1)}] The vectorized or scalar
result of the test, respectively.
}
\description{
Check if an object is \code{Inf}, \code{-Inf}, \code{NaN}, or none of them, flexibly dealing
with \code{NA} values.

The \verb{are_*()} functinos are vectorized, returning a vector of same length as
\code{x}, and errors for non-numeric objects. The \verb{is_*()} functions return
\code{FALSE} for non-numeric objects, or if not all elements pass the
corresponding \verb{are_*()} function, and \code{TRUE} otherwise.
}
\details{
Currently, \code{NaN} values can never arise from operations with \code{NA} (\code{NA + NaN #> NA}), so treating \code{NaN} as \code{NA} via \code{nan = "na"} is not recommended.
}
\examples{
x <- c(1, Inf, -Inf, NaN, NA)

# The default tests:
are_finite(x)
#> [1] TRUE  FALSE  FALSE  FALSE  NA

are_inf(x)
#> [1] FALSE  TRUE  TRUE  FALSE  NA

are_nan(x, na = "na")
#> [1] FALSE  FALSE  FALSE  TRUE  NA

# For all, the NA value's result can be controlled:
are_finite(x, na = "f")
#> [1]  TRUE FALSE FALSE FALSE FALSE

are_nan(x, na = "t")
#> [1] FALSE FALSE FALSE  TRUE  TRUE


# We can consider only +Inf or -Inf:
are_inf(x, signs = "+")
#> [1] FALSE  TRUE FALSE FALSE  NA


# The is_* predicates return TRUE only if all elements pass the test:
is_finite(1:10) #> TRUE
is_inf(c(-Inf, Inf, Inf, -Inf)) #> TRUE

# To test for a single value, use the n argument:
is_nan(NaN, n = 1) #> FALSE
is_nan(c(NaN, NaN), n = 1) #> FALSE

}
