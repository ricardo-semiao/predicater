% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests-general_asserts.R
\name{assert_from_msgs}
\alias{assert_from_msgs}
\alias{assert_from_error}
\title{Tests - Rethrow errors with more information}
\usage{
assert_from_msgs(
  fun,
  ...,
  args_fun = list(),
  x_names = NULL,
  env = caller_env(),
  args_abort = list()
)

assert_from_error(
  fun,
  ...,
  args_fun = list(),
  x_names = NULL,
  env = caller_env(),
  args_abort = list()
)
}
\arguments{
\item{fun}{[\verb{\\()\{\}}] For \code{assert_from_msg}: a function that checks an object
and returns \code{TRUE} if the check passes, or a message (\code{character(1)}) if it
fails; For \code{assert_from_error}: a function that checks an object and raises
an error if the check fails.}

\item{...}{[\code{any} each] Objects to check.}

\item{args_fun}{[\code{list()}] Additional arguments to pass to \code{fun}.}

\item{x_names}{[\code{character()} | \code{NULL}] The names of \code{...} to print in
messages. In \code{NULL}, the name is inferred from \code{x}'s expression.}

\item{env}{[\code{environment()}] The environment to use for the condition. Often
useful to remove this helper from the trace stack.}

\item{args_abort}{[\code{list()}] Additional arguments to pass to
\code{\link[cli:cli_abort]{cli::cli_abort()}}, which rethrows the error.}
}
\value{
[\code{TRUE}] Invisibly \code{TRUE}, or aborts if the check fails.
}
\description{
Packages like this one, \code{checkmate}, and \code{chk} provide functions to check
objects and return a message or abort if the check fails. This function
allows the user to catch that message/error and generate a condition with
higher flexibility than the original package.
}
\examples{
# Using checkmate::check_* functions:
# try({
#   f <- \(x) test_msg(checkmate::check_numeric, x, any.missing = FALSE)
#   f(c(1, NA))
# })
#> Error in `f()`:
#> ! Argument `x` contains missing values (element 2)

# Multiple arguments with the same test:
# try({
#   g <- \(x, y) test_msgs(checkmate::check_logical, x, y, args = list(len = 1))
#   g(TRUE, c(FALSE, TRUE))
# })
#> Error in `g()`:
#> ! Argument `y` must have length 1, but has length 2

}
