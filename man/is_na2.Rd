% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks-types-atomic_na.R
\name{is_na2}
\alias{is_na2}
\alias{are_na2}
\title{Type checks - NA values}
\usage{
are_na2(x, nan = FALSE)

is_na2(x, nan = FALSE, types = NULL)
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{nan}{[\code{TRUE} | \code{FALSE}] What to return for \code{NaN} values.}

\item{types}{[\code{character()} | \code{NULL}] A character vector of allowed \code{NA}
types (see \code{\link{NA}}). Set to \code{NULL} to allow all types.}
}
\value{
\itemize{
\item [\code{logical(length(x))}] For \verb{are_*}: the vectorized or result of the test.
\item [\code{TRUE} | \code{FALSE}] For \verb{is_*}: the scalar result of the test.
}
}
\description{
Checks if an object is \code{NA}, \code{NA_integer_}, \code{NA_real_}, \code{NA_complex_}, or
\code{NA_character_}. Differently from \code{\link[=is.na]{is.na()}} and
\code{\link[rlang:are_na]{rlang::is_na()}}/\code{\link[rlang:are_na]{rlang::are_na()}}, it allows to control the behavior for
\code{NaN} objects.
\itemize{
\item \code{are_na2()} is vectorized, returning a vector of same length as \code{x}, and
errors for non-atomic objects.
\item \code{is_na2()} returns \code{FALSE} for non-atomic
objects, or if not all elements pass \code{are_na2()}, and \code{TRUE} otherwise.
}
}
\examples{
x <- c(1, Inf, -Inf, NaN, NA)

# are_na() is vectorized:
are_na2(x)
#> c(FALSE, FALSE, FALSE, FALSE, TRUE)

are_na2(x, nan = TRUE)
#> c(FALSE, FALSE, FALSE, TRUE, TRUE)

are_na2(x, nan = NA)
#> c(FALSE, FALSE, FALSE, NA, TRUE)

# Errors for non-atomic objects:
try(are_na2(list(NA, NA))) #> Error

# is_na() test for a scalar NA value:
is_na2(c(NA, NA)) #> FALSE
is_na2(NA) #> TRUE
is_na2(NaN, nan = TRUE) #> TRUE

# Use all(are_na2(x)) to test if all elements are NA:
all(are_na2(c(NA, NA))) #> TRUE

# Accept only NAs of specific types:
is_na2(NA_integer_, types = c("logical", "character")) #> FALSE 

# To test for a single NA value, use the n argument:
is_na2(NA, n = 1) #> TRUE

}
