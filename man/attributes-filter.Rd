% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{attributes-filter}
\alias{attributes-filter}
\alias{attrs_rmv}
\alias{attrs_keep}
\alias{has_attrs_any}
\alias{has_attrs_all}
\alias{has_attrs_only}
\title{Attributes - Test for and filter attributes from object}
\usage{
attrs_rmv(x, abbr = "", exact = character(), match = character(), perl = FALSE)

attrs_keep(
  x,
  abbr = "",
  exact = character(),
  match = character(),
  perl = FALSE
)

has_attrs_any(x, abbr = "", exact = character(), disallow = character())

has_attrs_all(x, abbr = "", exact = character(), disallow = character())

has_attrs_only(x, abbr = "", exact = character())
}
\arguments{
\item{x}{[\code{any}] Object to remove attributes from.}

\item{abbr}{[\code{character(1)}] String of characters specifying attributes to
keep: \code{"n"} for 'names', \code{"d"} for 'dim', \code{"c"} for 'class', \code{"r"} for both
rownames and dimnames.}

\item{exact}{[\code{character()}] Other attributes to keep, specified by name.}

\item{match}{[\code{character()}] Other attributes to keep, specified by regex
pattern. They are combined with the or regex 'or' operator \code{|}.}

\item{perl}{[\code{TRUE} | \code{FALSE}] Whether to use perl-compatible regex.}

\item{disallow}{[\code{character()}] Attributes that, if present, will fail the
test.}
}
\value{
\itemize{
\item [\verb{=x}] For \code{attrs_rmv()} and \code{attrs_keep()}: Same object as \code{x} but with
attributes some removed.
\item [\code{TRUE} | \code{FALSE}] For \code{has_attrs_any()}, \code{has_attrs_all()}, and
\code{has_attrs_only()}: the scalar result of the test.
}
}
\description{
Functions to test for and filter attributes from an object. Attributes can be
selected by abbreviation in \code{abbr}, by name in \code{exact} or \code{disallow}, or by
regex pattern in \code{match}.

To remove attributes:
\itemize{
\item \code{attrs_rmv()} returns \code{x} without the attributes selected.
\item \code{attrs_keep()} returns \code{x} with only the attributes selected.
}

To test for attributes:
\itemize{
\item \code{has_attrs_any()} returns \code{TRUE} if any of the selected attributes are
present.
\item \code{has_attrs_all()} returns \code{TRUE} if all of the selected attributes are
present.
\item \code{has_attrs_only()} returns \code{TRUE} if all and only the selected attributes
are present.
}
}
\examples{
x <- structure(
  1:3, a = 1, b = 2, c = 3,
  names = c("X", "Y", "Z"), dim = c(1, 3), class = "myclass",
  levels = c("aa", "bb", "cc")
)

attrs_rmv(x) # Removes all attributes
attrs_rmv(x, abbr = "ncd") # Keeps names, dim, and class
attrs_rmv(x, abbr = "c", exact = c("levels", "b")) # Keeps class, levels, b
attrs_rmv(x, match = c("^[a-z]$")) # Keeps a, b, c

}
