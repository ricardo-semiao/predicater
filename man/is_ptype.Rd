% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ptype.R
\name{is_ptype}
\alias{is_ptype}
\alias{is_ptype_list}
\title{Check if object is of prototype}
\usage{
is_ptype(
  .x,
  .ptype,
  .typeof = "==",
  .length = "==_0n",
  .attrs = "no",
  class = "==_0n",
  dim = "==_0n",
  names = "==_0n",
  rownames = "==_0n",
  dimnames = "id_0n",
  ...
)

is_ptype_list(.x, .ptype, .named = TRUE, .depth = 1, ...)
}
\arguments{
\item{.x}{[\code{any}] Any R object.}

\item{.ptype}{[\code{any}] A prototype object that defines metadata to check
against \code{.x}.}

\item{.typeof, .length, .attrs}{[\code{character(1)} | \verb{\\(vx, vp) \{\}}]
How to check the non-attribute metadata -- \code{typeof()}, \code{length()}, and
attributes names -- of \code{.x} against \code{ptype}. Can be either: a function that
takes the metadata's value of \code{.x} (\code{vx}) and \code{.ptype} (\code{vp}) as arguments
and returns a boolean; or a string that specifies a predefined function (see
\href{#details}{Details}).}

\item{class, dim, names, rownames, dimnames, ...}{[\code{character(1)} | \verb{\\(vx, vp) \{\}}] How to check the attributes -- each
argument is an attribute name -- of \code{.x} against \code{.ptype}. With a function
or string (same as above).}

\item{.named}{[\code{TRUE} | \code{FALSE}] For \code{is_ptype_list()}, whether to use the
names of \code{.x} and \code{.ptype} for matching them (\code{TRUE}), or the order
(\code{FALSE}).}

\item{.depth}{[\code{integer(1)}] For \code{is_ptype_list()}, how many levels of
recursion to check. \code{1} means check the first level elements only.}
}
\description{
Check if \code{.x} has the same attributes as \code{.ptype}, with more flexibility than
\code{\link[vctrs:vec_assert]{vctrs::vec_is()}}. A prototype is a template object that defines metadata:
\code{\link[=typeof]{typeof()}}, \code{\link[=length]{length()}}, names of the attributes, and their values, everything
besides data. With \code{is_ptype()}, the user can specify which metadata to check
and how.

\code{is_ptype_list()} recursively checks if a list object has the correct
elements given a list prototype, useful when dealing with complex objects
like \code{list(a = integer(), b = list(c = character(), d = double()))}.
}
\details{
Predefined check functions:
\itemize{
\item \code{"no"}: don't check, always return \code{TRUE}.
\item \code{"id"}: check if \code{identical()}.
\item \code{"=="}: check if \code{all(equal())}.
\item \code{"id_0n"} and \code{"==_0n"}: same as above, but return \code{TRUE} if \code{vp} is \code{NULL}
or \code{0}.
\item \code{"id_ord"} and \code{"==_ord"}: same as above, but ignore the order of values.
\item \code{"id_sub"} and \code{"==_sub"}: same as above, but allow \code{vx} to be a subset of
\code{vp}.
}
}
\examples{
# By default, length is checked when the prototype's is not 0:
is_ptype(1:10, integer()) #> TRUE
is_ptype(1:10, integer(9)) #> FALSE

# You can change the default check for common attributes:
is_ptype(matrix(1:9, 3, 3), integer()) #> FALSE
is_ptype(matrix(1:9, 3, 3), integer(), dim = "no") #> TRUE

# Less common attributes' checks need to be specified in `...`:
is_ptype(
  factor(c("a", "b")), factor(levels = c("a", "b", "c")),
  levels = "=="
)
#> FALSE

# Complex objects can be checked with `is_ptype_list()`:
is_ptype_list(
  list(a = 1L, b = mtcars, c = rnorm(sample(1:10))),
  list(a = integer(1), b = data.frame(), c = double())
)
#> TRUE

}
