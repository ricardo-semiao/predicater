% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{attributes-getters-setters}
\alias{attributes-getters-setters}
\alias{names3}
\alias{names3<-}
\alias{dimnames2}
\alias{dimnames2<-}
\alias{rownames2}
\alias{rownames2<-}
\alias{class2}
\alias{class2<-}
\title{Attributes - Getters and setters}
\usage{
names3(x, repair = "unique", how = "names")

names3(x, repair = "unique", how = "names") <- value

dimnames2(x, repair = "unique", how = "dimnames")

dimnames2(x, repair = "unique", how = "dimnames") <- value

rownames2(x, repair = "unique", how = "dimnames")

rownames2(x, repair = "unique", how = "dimnames") <- value

class2(x, how = "class")

class2(x, how = "class") <- value
}
\arguments{
\item{x}{[\code{any}] Object to get or set attribute from.}

\item{repair}{Either a string or a function. If a string, it must be one of
\code{"check_unique"}, \code{"minimal"}, \code{"unique"}, \code{"universal"}, \code{"unique_quiet"},
or \code{"universal_quiet"}. If a function, it is invoked with a vector of
minimal names and must return minimal names, otherwise an error is thrown.
\itemize{
\item Minimal names are never \code{NULL} or \code{NA}. When an element doesn't
have a name, its minimal name is an empty string.
\item Unique names are unique. A suffix is appended to duplicate
names to make them unique.
\item Universal names are unique and syntactic, meaning that you can
safely use the names as variables without causing a syntax
error.
}

The \code{"check_unique"} option doesn't perform any name repair.
Instead, an error is raised if the names don't suit the
\code{"unique"} criteria.

The options \code{"unique_quiet"} and \code{"universal_quiet"} are here to help the
user who calls this function indirectly, via another function which exposes
\code{repair} but not \code{quiet}. Specifying \code{repair = "unique_quiet"} is like
specifying \verb{repair = "unique", quiet = TRUE}. When the \code{"*_quiet"} options
are used, any setting of \code{quiet} is silently overridden.}

\item{how}{[\code{character(1)}]
How to get or set the attribute:
\itemize{
\item For \code{names3()}: \code{"names"} for \code{\link[=names]{names()}} and \code{"attr"} for \code{\link[=attr]{attr()}}.
\item For \code{dimnames2()}: \code{"dimnames"} for \code{\link[=dimnames]{dimnames()}} and \code{"attr"} for \code{attr(x, "dimnames")}.
\item For \code{rownames2()}: \code{"dimnames"} for the first element of \code{\link[=dimnames]{dimnames()}},
\code{"attr"} for \code{attr(x, "dimnames")[[1]]}, and \code{"row.names"} for \code{attr(x, "row.names")}.
\item For \code{class2()}: \code{"class"} for \code{\link[=class]{class()}} and \code{"attr"} for \code{attr(x, "class")}.
}}

\item{value}{[\code{any}] Value to set the attribute to.}
}
\value{
\itemize{
\item [\code{character(length(x))} | \code{NULL}] For \code{names3()}.
\item [\code{list(length(dim(x)))} | \code{NULL}] For \code{dimnames2()}.
\item [\code{character(dim(x)[1])} | \code{NULL}] For \code{rownames2()}.
\item [\code{character()}] For \code{class2()}.
\item [\verb{=value}] For the setters: The \code{value} passed, invisibly.
}
}
\description{
Functions to get and set names, dimnames, and class of an object, while
requiring a specific 'quality' of the values.
}
\examples{
x <- structure(
  1:6, dim = c(2, 3, 1), names = paste0("i", 1:6),
  dimnames = list(c("a", "a"), c("x", "y", "z"), "_bad"),
  class = c("classA", "", NA, "classB", "classA")
)

# Getting repaired names and valid classes:
names3(x) #> c("i1", "i2", "i3", "i4", "i5", "i6") (no repair)

suppressMessages(dimnames2(x))
#> list(c("a...1", "a...2"), c("x", "y", "z"), "_bad")
suppressMessages(dimnames2(x, repair = "universal"))
#> list(c("a...1", "a...2"), c("x", "y", "z"), "._bad")

suppressMessages(rownames2(x)) #> c("a...1", "a...2")

class2(x) #> c("classA", "classB")

# Setting repaired names and valid classes:
class2(x) <- c("again", "duplicates", "again")
class2(x) #> c("again", "duplicates")

suppressMessages(rownames2(x) <- NULL) # Names are created automatically
rownames(x) #> c("...1", "...2")

}
