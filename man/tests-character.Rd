% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests-other_atomic.R
\name{tests-character}
\alias{tests-character}
\alias{test_double}
\alias{assert_double}
\title{Tests - Character}
\usage{
test_double(
  x,
  mode = "double",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sentinels = c(null = "f", na = "f", nan = "f", inf = "f", t = "f", f = "f"),
  sorted = NULL,
  custom = NULL
)

assert_double(
  x,
  action = "abort",
  mode = "double",
  len = NULL,
  na_n = NULL,
  dup_n = NULL,
  nan_n = NULL,
  inf_n = NULL,
  range = NULL,
  set = NULL,
  sentinels = c(null = "f", na = "f", nan = "f", inf = "f", t = "f", f = "f"),
  sorted = NULL,
  custom = NULL,
  env = caller_env(),
  x_name = NULL,
  report_untested = TRUE,
  ...
)
}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{len, na_n, dup_n}{}

\item{set}{[\code{character()} | \code{list(yes = , no = )} | \code{NULL}] Test if all values
of \code{x} are in a set of allowed values. Use a list with \code{yes} and \code{no}
elements to defined allowed and disallowed values. Set to \code{NULL} to not test.}

\item{sentinels}{[\code{character()}] A named character that allows \code{x} to be some scalar sentinel instead. By default, none are allowed. Add each named entry below to change the behaviour:
\itemize{
\item \code{null}: \code{"f"} to disallow \code{NULL}, \code{"t"} to allow.
\item \code{na}: \code{"f"} to disallow \code{NA}, \code{"t"} to allow \code{NA}, \code{"any"} to allow any     NA value.
\item \code{nan}: \code{"f"} to disallow \code{NaN}, \code{"t"} to allow.
\item \code{inf}: \code{"f"} to disallow, \code{"+"} to allow \code{+Inf}, \code{"-"} to allow \code{-Inf},     \code{"+-"} to allow both.
\item \code{t}: \code{"f"} to disallow \code{TRUE}, \code{"t"} to allow.
\item \code{f}: \code{"f"} to disallow \code{FALSE}, \code{"t"} to allow.
}}

\item{custom}{[\verb{function(x)} | \code{NULL}] A custom function that takes \code{x} as first argument   and returns a single \code{TRUE} or \code{FALSE}. Set to \code{NULL} to not test.}

\item{match}{[\code{character()} | \code{list(yes = , no = )} | \code{NULL}] Test if all
elements of \code{x} match regular expression patterns. Pass a character vector of
patterns (implicitly OR'd together), or a list with \code{yes} and \code{no}
character vectors of patterns. Set to \code{NULL} to not test.}

\item{perl}{[\code{TRUE} | \code{FALSE}] Should Perl-compatible regexps be used in
\code{match}?}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] for \code{test_character()}.
\item [\verb{=x}] for \code{assert_character()}, or aborts if the test fails.
}
}
\description{
Test if an object is a character vector.

\code{test_character()} is the predicate test, while \code{assert_character()} validates
its input, aborting if it fails the test.
}
