% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests-collections.R
\name{tests-list}
\alias{tests-list}
\title{Tests - List}
\arguments{
\item{x}{[\code{any}] An object to test.}

\item{len, null_n, empty_n, dup_n}{[\code{integer(1)} | \code{integer(2)} | \verb{\\()\{\}} | \code{NULL}]
Possible values for the length, number of \code{NULL} elements, number of empty
elements (\code{length(element) == 0}), and number of duplicates. The options are:
\itemize{
\item \code{NULL} to not test.
\item A single non-negative number to test for \code{. == range}.
\item A single negative number to test for \code{. == length(x) + range}.
\item A vector of two non-negative numbers to test for \verb{range[1] <= . <= range[2]}. \code{Inf} is allowed.
\item A function that receives the value to test (e.g. the number of NULLs) and
the length of \code{x}, and returns a single \code{TRUE} or \code{FALSE}.
}}

\item{sentinels}{[\code{character()}] A named character that allows \code{x} to be
some scalar sentinel instead. By default, none are allowed. Add each named
entry to change the behaviour:
\itemize{
\item \code{null} (\code{NULL}): \code{"f"} to disallow, \code{"t"} to allow.
\item \code{na} (\code{NA}): \code{"f"} to disallow, \code{"t"} to allow \code{NA}, \code{"any"} to
allow any NA value.
\item \code{nan} (\code{NaN}): \code{"f"} to disallow, \code{"t"} to allow.
\item \code{inf} (\code{Inf}): \code{"f"} to disallow, \code{"+"} to allow \code{+Inf}, \code{"-"} to allow
\code{-Inf}, \code{"+-"} to allow both.
\item \code{t} (\code{TRUE}): \code{"f"} to disallow, \code{"t"} to allow.
\item \code{f} (\code{FALSE}): \code{"f"} to disallow, \code{"t"} to allow.
}}

\item{custom}{[\verb{\\()\{\}} | \code{NULL}] A custom function that takes \code{x} as first
argument and returns a single \code{TRUE} or \code{FALSE}. Set to \code{NULL} to not test.}

\item{custom_map}{[\verb{\\()\{\}} | \code{NULL}] A custom function that is applied to
each element of \code{x}. Must return a single \code{TRUE} or \code{FALSE} for each element.
Set to \code{NULL} to not test.}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] for \code{test_list()}.
\item [\verb{=x}] for \code{assert_list()}, or aborts if the test fails.
}
}
\description{
Test if an object is a list.

\code{test_list()} is the predicate test, while \code{assert_list()} validates
its input, aborting if it fails the test.
}
