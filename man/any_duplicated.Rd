% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks-data.R
\name{any_duplicated}
\alias{any_duplicated}
\alias{are_duplicated}
\title{Data checks - Check for duplicate values}
\usage{
any_duplicated(x)

are_duplicated(x)
}
\arguments{
\item{x}{[\code{atomic()} | \code{list()}] An object to test.}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE}] For \code{any_duplicated()}: the scalar result of the test.
\item [\code{logical(length(x))}] For \code{are_duplicated()}: a logical vector of the same
size as \code{x}, describing if each element is duplicated elsewhere.
}
}
\description{
\itemize{
\item \code{any_duplicated}: is identical to \code{\link[vctrs:vec_duplicate]{vctrs::vec_duplicate_any()}}, and detects
the presence of duplicated values, similar to \code{\link[=anyDuplicated]{anyDuplicated()}}.
\item \code{are_duplicated()} is identical to \code{\link[vctrs:vec_duplicate]{vctrs::vec_duplicate_detect()}}, and
returns a logical vector describing if each element of the vector is
duplicated elsewhere. Unlike duplicated(), it reports all duplicated
values, not just the second and subsequent repetitions.
}
}
\section{Missing values}{


In most cases, missing values are not considered to be equal, i.e.
\code{NA == NA} is not \code{TRUE}. This behaviour would be unappealing here,
so these functions consider all \code{NAs} to be equal. (Similarly,
all \code{NaN} are also considered to be equal.)

}

\examples{
any_duplicated(1:10)       #> FALSE
any_duplicated(c(1, 1:10)) #> TRUE

x <- c(10, 10, 20, 30, 30, 40)
are_duplicated(x) #> c(TRUE, TRUE, FALSE, TRUE, TRUE, FALSE)
duplicated(x) #> c(FALSE, TRUE, FALSE, FALSE, TRUE, FALSE)
# Note that `duplicated()` ignores the first instance of a duplicated value

}
