% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other_functions.R
\name{reduce_predicate}
\alias{reduce_predicate}
\alias{accumulate_predicate}
\title{Reduce binary predicate over a list}
\usage{
reduce_predicate(.l, .f, ..., .op = "or")

accumulate_predicate(.l, .f, ..., .op = "or")
}
\arguments{
\item{.l}{[\code{list()}] A list of objects to compare.}

\item{.f}{[\verb{function()}] A binary predicate function, that uses their first
two arguments for the operation, and returns a single \code{TRUE} or \code{FALSE}.}

\item{...}{Additional arguments passed to \code{.f}.}

\item{.op}{[\code{"or"} | \code{"and"}] Whether to check if the predicate is \code{TRUE}
for any or any or all ordered pairs within \code{.l}.}
}
\value{
\itemize{
\item [\code{TRUE} | \code{FALSE} | \code{NA}] For \code{reduce_predicate()}: the scalar reduced
result.
\item [\code{logical(length(.l) - 1)}] For \code{accumulate_predicate()}: the accumulated
results.
}
}
\description{
This function applies a binary predicate function \code{.f} over a list \code{.l},
checking if the result is \code{TRUE} for any or all ordered pairs within \code{.l}.

\code{accumulate_predicate()} is similar, but returns a logical vector of the
accumulated results of the tests.
}
\examples{
reduce_predicate(list(1L, 1.0, 3), `==`, .op = "or") #> TRUE
reduce_predicate(list(1L, 1.0, 3), `==`, .op = "and") #> FALSE

accumulate_predicate(list(1L, 1.0, 3), `==`, .op = "and") #> c(TRUE, FALSE)
accumulate_predicate(list(1L, 1.0, 3), identical, .op = "and") #> c(FALSE, FALSE)

}
