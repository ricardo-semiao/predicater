% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert.R
\name{test_msg}
\alias{test_msg}
\alias{test_msgs}
\title{Abort using a message-generating function}
\usage{
test_msg(
  fun,
  x,
  ...,
  env = caller_env(),
  x_name = NULL,
  cnd_fun = cli_abort,
  cnd_args = list()
)

test_msgs(
  fun,
  ...,
  args = list(),
  env = caller_env(),
  x_names = NULL,
  cnd_fun = abort,
  cnd_args = list()
)
}
\arguments{
\item{fun}{[\verb{\\()\{\}}] A function that checks an object and returns \code{TRUE} if
the check passes, or a message if it fails.}

\item{x}{[\code{any}] The object to check.}

\item{...}{For \code{test_msgs()}, objects to check, for \code{test_msg()}, additional
arguments to pass to \code{fun}.}

\item{env}{[\code{environment()}] The environment to use for the condition. Often
useful to remove this helper form the trace stack.}

\item{x_name, x_names}{[\code{character(1)}, \code{character()} | \code{NULL}] The name of
\code{x} (or a vector of the names of \code{...} for \code{test_msgs()}) to print in
messages. In \code{NULL}, the name is inferred from \code{x}'s symbol, if possible.}

\item{cnd_fun}{[\verb{function()}] The function to use to generate the condition.
Defaults to \code{cli_abort()}.}

\item{cnd_args}{[\code{list()}] Additional arguments to pass to \code{cnd_fun}.}

\item{args}{[\code{list()}] For \code{test_msgs()}, additional arguments to pass to
\code{fun}.}
}
\value{
[\code{TRUE}] Invisibly \code{TRUE}, or aborts if the check fails.
}
\description{
Packages like \code{checkmate} provide functions to check objects and return a
message if the check fails. This function allows the user to catch that
message and generate a condition with higher flexibility than the original
package.

\code{test_msgs()} allows the user to apply the same test to multiple objects.
}
\examples{
# Using checkmate::check_* functions:
try({
  f <- \(x) test_msg(checkmate::check_numeric, x, any.missing = FALSE)
  f(c(1, NA))
})
#> Error in `f()`:
#> ! Argument `x` contains missing values (element 2)

# Multiple arguments with the same test:
try({
  g <- \(x, y) test_msgs(checkmate::check_logical, x, y, args = list(len = 1))
  g(TRUE, c(FALSE, TRUE))
})
#> Error in `g()`:
#> ! Argument `y` must have length 1, but has length 2

}
